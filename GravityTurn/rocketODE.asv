function du = rocketODE(u, rocket)
    v = u(1);
    gamma = u(2);
    x = u(3);
    h = u(4);
    m = u(5);
    du = zeros(5,1);
    
    T = rocket.Veff * rocket*dm;
    D = 1/2 * rocket.A * getDensity(h)* v^2*
end
